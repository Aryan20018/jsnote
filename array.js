let arr=[1,2,3,4,5,65,5]

function findmax(a){
    return  Math.max(...a);
}
// b=findmax(a);
// b= Math.max(...a);
// console.log(b);

let b=[]
function removeDuplicates(arr) {
 
    return arr. filter((item,index) => arr. indexOf(item) === index)
  }
  const c=removeDuplicates(arr);
// console.log(c);
// console.log(arr.indexOf(5));
// arr.filter((item,index)=>console.log(item,index))


